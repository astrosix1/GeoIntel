# Archive

Point-in-time development notes and feature announcements — useful as
history, not as current documentation (some describe steps or states that
no longer match the code, e.g. references to `python models.py` for DB
setup, now `alembic upgrade head`). For current setup and API docs, see
the root [README.md](../../README.md), [SETUP_GUIDE.md](../../SETUP_GUIDE.md),
and [backend/README.md](../../backend/README.md).

[**legacy-prototype/**](legacy-prototype/) holds unreferenced early-stage
HTML/JS/CSS pages (an earlier globe dashboard, a Three.js CDN check, a
manual API smoke test) — see its own README for what's there and why.
